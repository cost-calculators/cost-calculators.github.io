SHOPIFY COST CALCULATORS - v8  (NEOBRUTALIST BLACK & WHITE REDESIGN)
===================================================================
Live at: https://cost-calculators.github.io/

WHAT CHANGED - full visual redesign in a black & white "neobrutalist" style:
  * Thick 2px black borders + hard offset shadows on every card, button and input.
  * Press-effect buttons (shift + shadow shrink on hover).
  * Pill badges (rotated) + italic serif accents (Newsreader font).
  * Hand-drawn doodles (stars, sparkles, squiggles) + squiggle underlines.
  * Reveal-on-scroll entrance animations across sections.
  * Homepage now has a STACKING-CARD scroll effect ("Why these tools", 5 cards)
    and the 24 calculators are split into two labelled groups
    (Dev & project cost / Store & finance).
  * Pure black/white/grey palette throughout - no colour.

All 24 calculators still work exactly as before; only the look changed.

RE-UPLOAD: repo -> Add file -> Upload files -> drag everything here (overwrite)
-> Commit. Wait 1-2 min, then HARD-REFRESH (Ctrl+Shift+R). Assets are version-
stamped (?v=7) so the new look loads immediately.

Nothing else to configure. (Optional, unchanged: og/ images, Search Console.)
