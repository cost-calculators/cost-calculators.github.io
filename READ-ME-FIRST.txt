SHOPIFY COST CALCULATORS - v9  (neobrutalist B&W - CRASH FIXED)
==============================================================
Live at: https://cost-calculators.github.io/

FIXED IN THIS BUILD:
  * Homepage was hiding all 24 calculators. Cause: the scroll-reveal animation
    left cards invisible because the homepage didn't run the animation script.
    Reveal is now FAIL-SAFE (content shows even if the script doesn't run) and
    an inline reveal script runs on every page. All 24 cards show again.
  * The 5 big full-width blocks you saw were the "Why these tools" stacking
    cards (a scroll effect), not the calculators. The real 24 calculators sit
    below them in a 3-column grid.
  * Calculator pages: the Definition / formula / key boxes no longer stretch
    wide - they now match the text width, and the contents sidebar is clean.

Assets are version-stamped (?v=8), so the fixes load immediately.

RE-UPLOAD: repo -> Add file -> Upload files -> drag everything here (overwrite)
-> Commit. Wait 1-2 min, then HARD-REFRESH (Ctrl+Shift+R).
